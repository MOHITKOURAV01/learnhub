# Base URL of the LearnHub API.
# Vite only exposes variables prefixed with VITE_ to the client bundle.
# Defaults to http://localhost:5000 when unset, which matches the backend
# PORT in backend/.env.example.
VITE_API_BASE_URL=http://localhost:5000
